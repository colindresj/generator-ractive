language: node_js
node_js:
  - '0.10'
before_install:
  - currentfolder=${PWD##*/}
  - if [ "$currentfolder" != 'generator-ractive' ]; then cd .. && eval "mv $currentfolder generator-ractive" && cd generator-ractive; fi

