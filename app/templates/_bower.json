{
  "name": "<%= _.slugify(props.project) %>",
  "version": "0.0.0",
  "dependencies": {<% } if (includejQuery) { %>
    "jQuery": "~1.11.1", <% if (includeModernizr) { %>
    "modernizr": "~2.8.2", <% } %>
    "ractive": "~0.5.5"
  }
}

