{
  "name": "<%= _.slugify(props.project) %>",
  "version": "0.0.0",
  "dependencies": {<% if (includeModernizr) { %>
    "modernizr": "~2.8.2", <% } if (includejQuery) { %>
    "jQuery": "~1.11.1" <% } %>
  }
}

